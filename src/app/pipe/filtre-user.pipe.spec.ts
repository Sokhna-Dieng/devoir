import { FiltreUserPipe } from './filtre-user.pipe';

describe('FiltreUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreUserPipe();
    expect(pipe).toBeTruthy();
  });
});
