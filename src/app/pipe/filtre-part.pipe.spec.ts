import { FiltrePartPipe } from './filtre-part.pipe';

describe('FiltrePartPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrePartPipe();
    expect(pipe).toBeTruthy();
  });
});
